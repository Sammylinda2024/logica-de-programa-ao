/**sort(): Ordena os elementos de um array. Por padrão, o sort()
ordena os elementos como strings, o que pode causar uma
ordenação numérica incorreta.
 */

let numeros = [40,100,1,5,25,10];
numeros.sort();
console.log(numeros);

