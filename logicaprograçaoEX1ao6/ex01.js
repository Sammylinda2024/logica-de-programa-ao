// Cria um array com 10 números inteiros
let numeros = [3, 8, 15, 22, 7, 14, 10, 29, 6, 2];

// Itera pelo array e exibe apenas os números pares
for (let i = 0; i < numeros.length; i++) {
  if (numeros[i] % 2 === 0) {
    console.log(numeros[i]);
  }
}
